# PRECOMPUTE / OFFLINE lane — the heavy engines for the analysis toolkit + the forecasting ladder.
# Installed in .venv-pipeline312 (Python 3.12; the canonical offline venv since 2026-07-10 - ray ships
# cp312 win_amd64 wheels, so the REAL neuralforecast framework installs; py3.13-on-Windows cannot install ray).
# Pinned to the verified install. NOT Pyodide-safe;
# this lane runs offline (GPU where available) and bakes committed artifacts. The live/web lane uses only
# requirements.txt (numpy). See docs/analysis.md and plans/chronoscope (in _CAOS_MANAGE).
numpy==2.4.6
scipy==1.15.3
pandas==2.3.3
# --- analysis toolkit (understand the series) ---
statsmodels==0.14.6          # ADF/KPSS/Zivot-Andrews, STL/MSTL, ACF/PACF, Ljung-Box, filters, VECM
EMD-signal==1.9.0            # EMD/EEMD/CEEMDAN intrinsic mode functions (import PyEMD)
PyWavelets==1.8.0            # CWT scalogram (scipy.signal.cwt was removed in scipy 1.15)
# Deep tier: neuralforecast is the CANONICAL engine on the py3.12 base (engines/neuralforecast_engine.py);
# the direct-torch implementations (engines/neural_engine.py) remain the parity reference + CI fallback.
# Decision + revision: wip/chronoscope/deep-engine-decision-2026-07-04.md.
neuralforecast==3.1.9        # canonical deep tier (NHITS/DLinear/NLinear w/ MQLoss); pulls ray[train,tune]
ruptures==1.1.10             # PELT/BinSeg offline change-point detection
arch==8.0.0                  # Phillips-Perron, DF-GLS, GARCH family (statsmodels has no PP/DF-GLS)
antropy==0.2.2               # sample/permutation/spectral entropy + Higuchi/Katz/Petrosian fractal dimension
nolds==0.6.3                 # Hurst (R/S), DFA (+ corr-dim/Lyapunov for the nonlinear-dynamics unit)
hurst==0.0.5                 # Hurst R/S (Anis-Lloyd corrected)
MFDFA==0.4.3                 # multifractal DFA singularity spectrum
# pycatch22 (Lubba 2019, 22 canonical features): OPTIONAL, needs a C toolchain; recorded honestly if absent
preqts==0.3.0                # OUR published package (PyPI): prequential eval + ACI/Conformal-PID + MSIS + per-horizon curves
# --- forecasting ladder (classical / statistical / ML / foundation) ---
statsforecast==2.0.3         # AutoARIMA/AutoETS/AutoTheta, MSTL, TBATS, Croston, GARCH
mlforecast==1.0.31           # LightGBM on lag/date features
utilsforecast==0.2.16        # Guerrero Box-Cox lambda, scalers
coreforecast==0.0.17
lightgbm==4.6.0
chronos-forecasting==2.3.1   # Chronos / Chronos-Bolt / Chronos-2 (Apache-2.0 foundation tier)
# torch: install the CUDA build from the cu126 index (driver 560.94 = CUDA 12.6, verified 2026-07-04):
#   pip install torch==2.12.1+cu126 --index-url https://download.pytorch.org/whl/cu126
# CPU-only clones/CI: plain `pip install torch==2.12.1` works (the engines fall back to CPU via chronoscopelab.gpu).
torch==2.12.1                # backbone for the foundation + deep engines (GPU offline lane; cu126 wheel on the build box)
