// Architecture / "How it works" modal config (ADR-0058).
// COPY this file to frontend/src/architecture.ts when the shell-based frontend is built (later slice),
// fill the SVGs, and pass it to the AppShell: <AppShell config={{ ...shellConfig, architecture }}>.
// Shipped as .ts.txt so it does NOT enter the current minimal replay SPA's tsc build (no shell dependency yet).
//
// The five tabs are the ADR-0058 minimum: (1) the app, (2) lanes, (3) web flow, (4) the science,
// (5) the data contracts. The bilingual en/es copy is user-facing UI (i18n, ADR-0011); English is canonical.
import type { ArchitectureConfig } from '@fasl-work/caos-app-shell';

export const architecture: ArchitectureConfig = {
  tabs: [
    {
      id: 'app',
      en: 'The app',
      es: 'La app',
      svg: 'svg/tech/01-the-app.svg',
      body_en:
        'ChronoScope is a workbench for time-series forecasting itself. It takes a series (a built-in case or ' +
        'your own data), runs the full method ladder on it, and shows each forecast against the held-out truth ' +
        'with honest backtest metrics (MASE, WQL, interval coverage), so you can see where each family of ' +
        'methods is strong and where it breaks.\n\n' +
        'It is a real system, not a demo: the classical forecasters recompute live in the browser, an offline ' +
        'pipeline with two data contracts lets you run it on your own series, and every forecast is scored by ' +
        'a leakage-safe prequential backtest.',
      body_es:
        'ChronoScope es un banco de trabajo para el pronostico de series de tiempo en si mismo. Toma una serie ' +
        '(un caso incluido o tus propios datos), corre sobre ella la escalera completa de metodos, y muestra ' +
        'cada pronostico contra la verdad reservada con metricas honestas de backtest (MASE, WQL, cobertura del ' +
        'intervalo), para ver donde cada familia de metodos es fuerte y donde falla.\n\n' +
        'Es un sistema real, no un demo: los pronosticadores clasicos recalculan en vivo en el navegador, un ' +
        'pipeline offline con dos contratos de datos permite correrlo con tus propias series, y cada pronostico ' +
        'se evalua con un backtest prequencial sin fuga de informacion.',
    },
    {
      id: 'lanes',
      en: 'Lanes: web / offline / compute',
      es: 'Carriles: web / offline / computo',
      svg: 'svg/tech/02-lanes.svg',
      body_en:
        'Three lanes, and the split is the point. WEB (live, in the browser): the pure-numpy classical core ' +
        '(seasonal-naive, SES, Holt, Holt-Winters, Theta) recomputes on every control, no server. OFFLINE ' +
        '(your machine, isolated .venv-pipeline): the Python pipeline bakes the canonical artifacts and scores ' +
        'every method with the preqts prequential library; later slices add the heavy engines (Nixtla, ' +
        'LightGBM, zero-shot foundation models) here. REPLAY: the small committed artifacts in data/derived are ' +
        'overlaid into the SPA and loaded live; the typed mirror (contract.types.ts) fails the build if the web ' +
        'and pipeline shapes ever diverge.',
      body_es:
        'Tres carriles, y la division es lo central. WEB (en vivo, en el navegador): el nucleo clasico en ' +
        'numpy puro (naive-estacional, SES, Holt, Holt-Winters, Theta) recalcula con cada control, sin ' +
        'servidor. OFFLINE (tu maquina, .venv-pipeline aislado): el pipeline Python hornea los artefactos ' +
        'canonicos y evalua cada metodo con la libreria prequencial preqts; slices posteriores agregan aqui los ' +
        'motores pesados (Nixtla, LightGBM, modelos fundacionales zero-shot). REPLAY: los artefactos pequenos y ' +
        'versionados en data/derived se superponen al SPA y se cargan en vivo; el espejo tipado ' +
        '(contract.types.ts) rompe el build si la web y el pipeline divergen.',
    },
    {
      id: 'web-flow',
      en: 'Web-app flow',
      es: 'Flujo de la web',
      svg: 'svg/tech/03-web-flow.svg',
      body_en:
        'The App page selects a case and a method: it loads the committed trace and renders the history, the ' +
        'held-out truth, and the chosen method with its prediction interval, plus a table of per-method backtest ' +
        'metrics. The six sibling pages (App, Introduction, Methodology, Implementation, Experiments, Benchmark) ' +
        'follow the shared CAOS structure. The build is gated by the contract-type mirror, artifacts are ' +
        'overlaid at build time, vite builds the static output, and GitHub Pages serves it at ' +
        'chronoscope.fasl-work.com.',
      body_es:
        'La pagina App selecciona un caso y un metodo: carga la traza versionada y dibuja el historial, la ' +
        'verdad reservada y el metodo elegido con su intervalo de prediccion, mas una tabla de metricas de ' +
        'backtest por metodo. Las seis paginas hermanas (App, Introduccion, Metodologia, Implementacion, ' +
        'Experimentos, Benchmark) siguen la estructura CAOS compartida. El build lo controla el espejo de tipos ' +
        'del contrato, los artefactos se superponen al construir, vite genera el estatico y GitHub Pages lo ' +
        'sirve en chronoscope.fasl-work.com.',
    },
    {
      id: 'science',
      en: 'The science',
      es: 'La ciencia',
      svg: 'svg/tech/04-the-science.svg',
      body_en:
        'The method ladder, from a transparent baseline upward: seasonal-naive (the MASE denominator), simple ' +
        'and double exponential smoothing (SES, Holt), Holt-Winters additive seasonality, and the Theta method ' +
        '(SES with half the OLS drift). Each is a real state-space or decomposition model, not a curve fit. ' +
        'Every method is scored by a leakage-safe rolling-origin backtest (preqts): MASE for point accuracy, ' +
        'WQL for the full quantile forecast, and empirical coverage against the nominal interval. Later slices ' +
        'add the ML, deep, and zero-shot foundation-model tiers behind the same contract.',
      body_es:
        'La escalera de metodos, desde un baseline transparente hacia arriba: naive-estacional (el denominador ' +
        'de MASE), suavizamiento exponencial simple y doble (SES, Holt), estacionalidad aditiva Holt-Winters, y ' +
        'el metodo Theta (SES con la mitad de la pendiente OLS). Cada uno es un modelo real de espacio de ' +
        'estados o de descomposicion, no un ajuste de curva. Cada metodo se evalua con un backtest de origen ' +
        'movil sin fuga (preqts): MASE para la exactitud puntual, WQL para el pronostico de cuantiles completo, ' +
        'y cobertura empirica frente al intervalo nominal. Slices posteriores agregan los niveles ML, profundo ' +
        'y de modelos fundacionales zero-shot bajo el mismo contrato.',
    },
    {
      id: 'design',
      en: 'Data contracts / design',
      es: 'Contratos de datos / diseno',
      svg: 'svg/tech/05-data-contracts.svg',
      body_en:
        'Two validated data contracts bracket the pipeline. Contract 1 (ingestion) defines a valid series: a ' +
        'long-format unique_id/ds/y table with a missing/outlier policy (reject too-short or too-missing series; ' +
        'flag outliers and unsorted timestamps), so the app accepts your data, not just the built-in cases. ' +
        'Contract 2 (artifact) defines the output the web reads (the per-case trace and the manifest index), ' +
        'mirrored exactly by contract.types.ts. Between them the staged, deterministic pipeline runs the lane ' +
        'gate and writes a provenance manifest, so every result is reproducible and the web can never silently ' +
        'drift from the pipeline.',
      body_es:
        'Dos contratos de datos validados encierran el pipeline. El Contrato 1 (ingesta) define una serie ' +
        'valida: una tabla en formato largo unique_id/ds/y con una politica de faltantes/atipicos (rechaza ' +
        'series muy cortas o muy incompletas; marca atipicos y marcas de tiempo desordenadas), para que la app ' +
        'acepte tus datos, no solo los casos incluidos. El Contrato 2 (artefacto) define la salida que lee la ' +
        'web (la traza por caso y el indice de manifiestos), espejada exactamente por contract.types.ts. Entre ' +
        'ambos, el pipeline por etapas y determinista corre el lane gate y escribe un manifiesto de procedencia, ' +
        'de modo que cada resultado es reproducible y la web nunca diverge en silencio del pipeline.',
    },
  ],
};
